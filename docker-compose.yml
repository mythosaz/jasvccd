services:
  quick-snip:
    build: .
    image: quick-snip:latest
    network_mode: bridge
    ports:
      - "8207:8000"
    volumes:
      - jas_data:/app/uploads
      - jas_out:/app/outputs
    restart: unless-stopped

volumes:
  jas_data:
  jas_out:
